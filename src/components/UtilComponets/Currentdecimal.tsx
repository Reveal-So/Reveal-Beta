
export function Currentdecimal(current:any){
   // console.log("current",current.toFixed(0) );
    return current.toFixed(0);
    // let cad = `${current}`.split('.');
    // if (!cad){ return <>N/A</>;}
    // return cad[0];
}